const allConstellations = [
    'Orion', 'Lyra', 'Cassiopeia', 'Draco', 'Ursa Major', 'Cygnus', 'Scorpius', 'Andromeda', 'Pegasus'
]
const questions = [
    {image:'',
        correct:'Orion',
    }
];

// need to figure an efficient way for questions (api maybe?)

let current = 0;
let score = 0;
function getRandomChoices(correctAnswer){
    const coices = new Set([correctAnswer]);
    while (choices.size<4){
        const random = 
    }
}

function showQuestion(){
    const q = questions[current];
    
}